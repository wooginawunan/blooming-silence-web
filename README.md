# Blooming Silence — Web (GitHub Pages)
Static frontend for image→motion demo. Deployed via GitHub Pages (root).

## Dev
Just edit `index.html`. No build step.

## Deploy
Push to `main`. Pages is set to "Deploy from branch" → main → /(root).
